spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.48.110:3306/gulimall_pms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  # 凡是返回日期的都会进行格式化
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  # 关闭thymeleaf缓存
  thymeleaf:
    cache: false


mybatis-plus:
  # classpath*表示不止扫描自己类路径下的mapper文件，也会扫描所引用的依赖中的jar包中的类路径也
  mapper-locations: classpath:mapper/**/*.xml
  # 设置所有主键自增
  global-config:
    db-config:
      id-type: auto
      # 配置逻辑删除标志
      logic-delete-value: 1
      logic-not-delete-value: 0
# 配置端口号
server:
  port: 10001
logging:
  level:
    com.syong.gulimall: debug
#配置阿里云oss信息(不兼容)
#alibaba:
#  cloud:
#    access-key: LTAI5tCRwrfdF8KACAg8SQt9
#    secret-key: 9AR7CxiXdQLjFlGiwN8DT9h4mGp9Bq
#    oss:
#      endpoint: oss-cn-hangzhou.aliyuncs.com


